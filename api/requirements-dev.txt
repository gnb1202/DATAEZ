# Development / test dependencies.
# Install alongside runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.3.4
httpx==0.28.1  # required by fastapi.testclient.TestClient
PyYAML==6.0.2  # golden eval dataset (app/eval/golden/*.yaml)
pytest-asyncio==0.25.0  # streaming agent tests
jsonschema==4.25.1  # actual function-call arguments in agent-quality evaluation
